# Pinned for a reproducible Streamlit Community Cloud build. Unpinned ">=" let the
# Cloud re-resolve to brand-new majors (pandas 3.0, numpy 2.5) whose fresh Linux
# wheels segfaulted the app on startup. These are stable versions with Python 3.12
# wheels. Deploy with Python 3.12 selected in Advanced settings (Cloud default is
# 3.12); Python cannot be changed in place, so a version bump needs a redeploy.
streamlit==1.40.2
pydeck==0.9.1         # real basemap under the float trajectory
plotly==5.24.1
pandas==2.2.3
numpy==1.26.4
xarray==2024.9.0
netCDF4==1.7.1
pyarrow==17.0.0
requests==2.32.3
tqdm==4.66.5
gsw==3.6.19          # TEOS-10 derived quantities (density, MLD, AOU, T-S)
scipy==1.13.1        # Mann-Kendall significance test
