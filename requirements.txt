# Pinned for a reproducible Streamlit Community Cloud build. Unpinned ">=" let the
# Cloud re-resolve to brand-new majors (pandas 3.0, numpy 2.5) whose fresh Linux
# wheels segfaulted the app on startup. The native packages are pinned to stable
# versions with Python 3.12 wheels; streamlit stays at the version the app was
# built and tested on (it is pure Python, not part of the segfault, and the app
# uses newer APIs like st.iframe and width="stretch"). Deploy with Python 3.12
# in Advanced settings (Cloud default is 3.12); Python cannot be changed in
# place, so a version bump needs a delete + redeploy.
streamlit==1.58.0
pydeck==0.9.1         # real basemap under the float trajectory
plotly==6.8.0
pandas==2.2.3
numpy==1.26.4
xarray==2024.9.0
netCDF4==1.7.1
pyarrow==17.0.0
requests==2.32.3
tqdm==4.66.5
gsw==3.6.19          # TEOS-10 derived quantities (density, MLD, AOU, T-S)
scipy==1.13.1        # Mann-Kendall significance test
